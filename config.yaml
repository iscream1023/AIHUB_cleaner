

path:
  src_root: "/home/haggi/fire_detection_datasets/AI-HUB segmentation/my_datasets"             # original data's high-level directory
  dst_root: "/home/haggi/fire_detection_datasets/AI-HUB segmentation/temp_datasets"           # temporary output dictory
  raw_labels: "/home/haggi/fire_detection_datasets/AI-HUB segmentation/temp_datasets/labels"  # FolderManager output dictory
  output_dir: "/home/haggi/fire_detection_datasets/AI-HUB segmentation/yolo_datasets"         # result output dictory

# 해싱 유사도 설정
dedup:
  hash_size: 8   # 해시 정밀도 (기본 8)
  threshold: 2   # 👈 유사도 설정 (0에 가까울수록 '완전 똑같은' 것만 지움, 클수록 '비슷한' 것도 지움)

class_mapping:
  1: 1
  2: 1
  3: 0

mode: "seg"